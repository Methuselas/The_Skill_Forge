# SkillForge Claude Instructions

Follow `AGENTS.md` for repository-wide architecture and validation rules. Repo skills
are available under `.claude/skills/`; their canonical portable/domain content lives
in `PASS/` and `library/`.

Do not treat `.claude/` as part of a finished skill release. Do not modify the Art
Stages unless the user explicitly starts the Stage-streamlining work.
