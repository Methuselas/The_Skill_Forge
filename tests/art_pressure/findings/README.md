# Findings

One structured finding file is added per bounded batch. Findings distinguish
library defects from retrieval, application, continuity, reference, tool, and
interface failures. Interpretive conflicts remain `NEEDS_MORE_EVIDENCE`.
