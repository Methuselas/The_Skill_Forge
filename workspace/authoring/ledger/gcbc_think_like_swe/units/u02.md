# u02 — Ch.2 Layers of abstraction

read:        2026-07-30
second_read: 2026-07-30
ledger_format: 2
candidate_count: 12

read_note: Code listings 2.1-2.9 extracted as complete, legible source text
(null-safety `?` convention, sendOwnerALetter, TextSummarizer monolith and its
one-class-per-concept refactor, the TextImportanceScorer interface with
WordBasedScorer/ModelBasedScorer and factory functions, the too-thin
OffsetDetector split). Figures 2.1-2.7 are dependency diagrams; their captions
and the surrounding prose both extracted cleanly and fully describe them. Visual
rendering was unavailable (pdftoppm not installed; render_pdf.py and Read's PDF
path both depend on it) — proceeded on the proxy evidence above per PASS_RUN §3,
extracting concepts rather than reproducing exact code. No caption referenced an
image whose content was absent from the text.

| candidate | type | disposition | object_id | grounding | learner_decision | variant_basis | method_or_policy | tradeoff | note |
|---|---|---|---|---|---|---|---|---|---|
| Decompose a problem into clean layers of abstraction | pattern | new | PAT_decompose_into_layers_of_abstraction | pp. 25-27 HttpConnection 3-lines/4-concepts, fig 2.1 | | | | | new foundation; related_to modularity foundation, distinct decision (recursive concept-count decomposition vs interface coupling) |
| Expose a clean API and hide implementation details | pattern | new | PAT_expose_clean_api_hide_implementation | pp. 29-30 fig 2.3 API vs impl split, leak test | | | | | new foundation |
| Make each function read like a single sentence | pattern | new | PAT_write_functions_as_single_sentences | pp. 30-32 sendOwnerALetter listings 2.2/2.3 | | | | | new; related_to readability foundation, distinct (function extraction/sentence test) |
| Size classes by the pillars, not line count | pattern | new | PAT_size_classes_by_pillars_not_lines | pp. 33-41 TextSummarizer, cohesion/SoC, listings 2.4/2.5 | | | | | new; cohesion + separation-of-concerns folded in as the grouping lens |
| Don't widen the public API just to reuse or test internals | pattern | new | PAT_dont_widen_api_for_reuse_or_testing | pp. 37-38 splitIntoParagraphs / "visible for testing" | | | | | new; sharpens API pattern at the moment engineers break it |
| Represent a layer with an interface when it earns its keep | pattern | new | PAT_use_interfaces_for_swappable_layers | pp. 41-45 TextImportanceScorer interface, listings 2.6-2.8, benefits/costs | | | | | merged the multi-impl rule and the single-impl "interfaces for everything?" judgment into one pattern |
| Tune layer thickness and err thin | pattern | new | PAT_tune_layer_thickness_err_thin | pp. 45-46 OffsetDetector too-thin, listing 2.9, err-thin rule | | | | | new |
| Signal absent values with null safety or optionals | pattern | new | PAT_prefer_null_safety_or_optionals | pp. 24-25 `?` convention, Optional types | | | | | new foundation under error-handling; ch4/ch6 nullable-return will link back |
| Keep clean layers inside microservices | pattern | new | PAT_keep_clean_layers_inside_microservices | pp. 47-48 stock service subproblems, cross-team reuse | | | | | new; corrects "microservices make internal structure irrelevant" |
| Translate functions into sentences and split clunky ones | drill | new | DRILL_translate_functions_into_sentences | pp. 30-32 sendOwnerALetter | | | | | teaches PAT_write_functions_as_single_sentences |
| Evaluate a big class against the pillars and split by concept | drill | new | DRILL_split_a_too_big_class_by_concept | pp. 35-41 TextSummarizer refactor | | | | | teaches PAT_size_classes_by_pillars_not_lines |
| Refactor a monolithic class into clean layered abstractions | ap | new | AP_refactor_monolithic_class_into_layers | pp. 35-46 TextSummarizer listings 2.4→2.5→2.6→2.7, OffsetDetector stop | | | | | staged refactoring workflow; DI + interface extraction preview ch8 |

## Notes

Rejected/absorbed as source facts (no separate rows):
- The dependency-graph vocabulary (functions/classes/interfaces/packages as units,
  fig 2.2) is definitional framing; the transferable content is in the specific
  function/class/interface patterns above. Packages/namespaces are explicitly out
  of the book's scope (p. 29).
- Cohesion (sequential/functional) and separation of concerns are conceptual
  vocabulary; folded into PAT_size_classes_by_pillars_not_lines as the grouping
  lens rather than extracted as standalone patterns.
- The four pillar-benefit paragraphs (readability/modularity/reusability/testability
  of layering, pp. 27-28) restate the Ch.1 pillar foundations applied to layering;
  captured in the Notes of PAT_decompose_into_layers_of_abstraction, not duplicated.

Placement: all layer-of-code patterns, both drills, and the AP live under the new
`abstraction` topic; null safety lives under `error-handling` so chapter 4's
error-signaling specializations join it. Cross-links point at both new objects and
Ch.1 foundations (modularity, readability), all of which resolve. Dependency
injection and interface extraction are previewed here and will be specialized in
chapter 8 — those objects should link back to AP_refactor_monolithic_class_into_layers
and PAT_use_interfaces_for_swappable_layers via foundation_object_id.

## Reading receipt

Verbatim evidence that the unit text was actually read, verified against the
payload by `tools/verify_grounding.py`. Pages are the book's printed page numbers
(the tool applies `pdf_page_offset` from SOURCE.md). Quotes must appear verbatim
in the extracted page text and span the whole unit, not just its opening.

| page | evidence |
|---|---|
| 27 | "we can tame it by identifying the subproblems and creating the correct layers of abstraction" |
| 33 | "It serves only as a warning that something might be wrong but is not an assurance that anything is right" |
| 38 | "Currently all that can be tested is the overall behavior via the summarizeText() function" |
| 44 | "We may have guessed wrong about only needing one implementation" |
| 47 | "the microservices themselves provide the clean layer of abstraction" |
