# u03 — Ch.3 Other engineers and code contracts

read:        2026-07-30
second_read: 2026-07-30
ledger_format: 2
candidate_count: 9

read_note: Listings 3.1-3.5 (the UserSettings class evolving from lots-of-small-
print → impossible-to-misuse via static factory/private constructor → checks →
assertion) extracted as complete, legible source text. Figures 3.1-3.5 are
diagrams (dependency snapshot, scooter-rental contract, misuse-path flowcharts);
their captions and surrounding prose extracted cleanly and fully convey them.
Visual rendering unavailable in this environment (pdftoppm not installed;
render_pdf.py and Read's PDF path both need it) — proceeded on the proxy evidence
above per PASS_RUN §3, extracting concepts not exact code. No caption referenced
an image whose content was missing from the text.

| candidate | type | disposition | object_id | grounding | learner_decision | variant_basis | method_or_policy | tradeoff | note |
|---|---|---|---|---|---|---|---|---|---|
| Convey usage through names and types, not docs | pattern | new | PAT_convey_usage_through_names_and_types | pp. 53-55 five channels, reliability ranking | | | | | new foundation |
| Make breakage fail at compile time or fail a test | pattern | new | PAT_make_breakage_fail_compile_or_test | pp. 52-53 "only two reliable ways" | | | | | new foundation; keystone rule |
| Identify your code's contract explicitly | pattern | new | PAT_define_your_code_contract_explicitly | pp. 55-56 pre/post/invariants, design-by-contract | | | | | new foundation |
| Prefer unmistakable contract terms over small print | pattern | new | PAT_prefer_unmistakable_over_small_print | pp. 57-58 scooter T&C, unmistakable vs small print channels | | | | | central pattern; related_to Ch1 hard-to-misuse + mental-model foundations |
| Make misuse impossible by removing invalid states | pattern | new | PAT_make_misuse_impossible_by_removing_invalid_states | pp. 60-62 UserSettings factory/private ctor, listing 3.3 | | | | | specializes hard-to-misuse; previews ch7 immutability |
| Enforce contracts at runtime with loud checks | pattern | new | PAT_enforce_contracts_at_runtime_with_checks | pp. 63-66 checks, listing 3.4; assertions listing 3.5 | Runtime-enforce a contract that can't be compile-enforced | constraint | Assertions instead of hand-written checks | Assertions compile out of release, trading production protection for perf/availability | assertions absorbed as variant VAR_assertions |
| Classify contract terms as unmistakable vs small print | drill | new | DRILL_classify_contract_terms_unmistakable_vs_small_print | pp. 58-60 UserSettings contract dissection | | | | | teaches PAT_define_your_code_contract_explicitly |
| Refactor a small-print class to be impossible to misuse | drill | new | DRILL_refactor_small_print_class_to_impossible_to_misuse | pp. 60-62 UserSettings redesign | | | | | teaches PAT_make_misuse_impossible_by_removing_invalid_states |
| Harden a code contract from small print to enforcement | ap | new | AP_harden_a_code_contract | pp. 58-66 UserSettings progression + checks/assertions ladder | | | | | escalation workflow: impossible → checks → assertions → document |

## Reading receipt

Verbatim evidence that the unit text was actually read, verified against the
payload by `tools/verify_grounding.py`. Pages are the book's printed page numbers
(the tool applies `pdf_page_offset` from SOURCE.md). Quotes must appear verbatim
in the extracted page text and span the whole unit, not just its opening.

| page | evidence |
|---|---|
| 52 | "your code in fact lives on top of constantly shifting foundations" |
| 57 | "The first two of the terms in the small print are not really that surprising" |
| 61 | "it's better to just make it impossible to do the wrong thing rather than relying on small print" |
| 65 | "Exceptions might be caught at some higher level in the program and just logged to prevent full on crashes" |

## Notes

The assertions material was absorbed as a variant of the runtime-checks pattern
rather than a separate object: same learner decision (runtime-enforce a contract
that cannot be compile-enforced), different method and tradeoff (assertions
normally compile out of release). Recorded per the decision-versus-method recovery
check.

Rejected/absorbed as source facts (no separate rows):
- Section 3.1 (things obvious to you aren't to others; others will break your
  code; you'll forget your own code) is motivational framing; its one transferable
  rule — breakage must fail compile or a test — was extracted as its own pattern,
  and "your future self is another engineer" is captured in that pattern's Notes
  and the names/types pattern.
- "Fuzz testing" sidebar (p. 65) is a definition; captured in the checks pattern's
  Notes as why checks raise what fuzzing can find, not a standalone object.
- The state/mutability note (p. 62) is a forward reference to chapter 7; folded
  into PAT_make_misuse_impossible_by_removing_invalid_states.

Cross-links point at Ch1 foundations (PAT_make_code_hard_to_misuse,
PAT_match_caller_mental_model) and the Ch2 null-safety pattern
(PAT_prefer_null_safety_or_optionals), all of which resolve. Chapters 6 and 7 will
add specific specializations that should link back to
PAT_prefer_unmistakable_over_small_print and
PAT_make_misuse_impossible_by_removing_invalid_states.
